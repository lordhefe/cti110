
#lists [] changeable, 0 indexed ,allow duplicates

numbers = [4,5,6,7,8,9,1]

# tuples (), NOT changeabke, 0 indexed, allow duplicates

numTuple = tuple(numbers)

# dictionaries {}, chanfable, duplicated values NOT keys. not 0 indexed
#unique pointer is the key

#set {}, changeable, DOES NOT allow duplicates, NOT 0 indexed

